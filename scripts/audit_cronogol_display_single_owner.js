#!/usr/bin/env node
let f=[];function a(c,m){if(!c)f.push(m)}const states=["RUNNING_LOCAL","RUNNING_REMOTE","STOP_FROZEN","WAITING_REMOTE","SPECIAL_PENDING","FINISHED"];a(new Set(states).size===states.length,"estados únicos");function owner(o){if(o.finished)return"FINISHED";if(o.localRunning)return"RUNNING_LOCAL";if(o.remoteRunning)return"RUNNING_REMOTE";if(o.special)return"SPECIAL_PENDING";if(o.hasStop)return"STOP_FROZEN";return"WAITING_REMOTE"}a(owner({localRunning:true})==="RUNNING_LOCAL","local running");a(owner({remoteRunning:true,hasStop:true})==="RUNNING_REMOTE","remote running");a(owner({hasStop:true})==="STOP_FROZEN","stop frozen");a(owner({finished:true,remoteRunning:true})==="FINISHED","finished manda");
console.log(`AUDITORIA_CRONOGOL_DISPLAY_SINGLE_OWNER: ${f.length?"FAIL":"OK"}`);if(f.length){f.forEach(x=>console.error("- "+x));process.exit(1)}
