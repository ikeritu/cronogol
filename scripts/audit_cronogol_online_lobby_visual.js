#!/usr/bin/env node
const fs=require("fs"),path=require("path");const root=process.cwd();const css=fs.readFileSync(path.join(root,"style.css"),"utf8");const game=fs.readFileSync(path.join(root,"game.js"),"utf8");let f=[];function a(c,m){if(!c)f.push(m)}a(css.includes("Online Lobby Visual Match"),"CSS block missing");a(css.includes(".online-lobby"),".online-lobby missing");a(css.includes(".online-lobby-badge"),"badge missing");a(game.includes("Online Lobby Visual Enhancer"),"enhancer missing");console.log(`AUDITORIA_CRONOGOL_ONLINE_LOBBY_VISUAL: ${f.length?"FAIL":"OK"}`);if(f.length){f.forEach(x=>console.error("- "+x));process.exit(1)}
