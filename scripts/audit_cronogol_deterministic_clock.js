#!/usr/bin/env node
const fs=require("fs"),path=require("path");const game=fs.readFileSync(path.join(process.cwd(),"online-clock.js"),"utf8");let f=[];function a(c,m){if(!c)f.push(m)}a(game.includes("CronoGolDeterministicOnlineClock"),"Debe existir CronoGolDeterministicOnlineClock");a(game.includes("requestAnimationFrame"),"Debe usar requestAnimationFrame");a(game.includes("runningState"),"Debe usar runningState");a(game.includes("startedAtMs"),"Debe usar startedAtMs");a(game.includes("publishStart"),"Debe tener publishStart");a(game.includes("publishStop"),"Debe tener publishStop");console.log(`AUDITORIA_CRONOGOL_DETERMINISTIC_CLOCK: ${f.length?"FAIL":"OK"}`);if(f.length){f.forEach(x=>console.error("- "+x));process.exit(1)}
